on: 
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  server-1:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 SERVER JASERBA
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.jaserba.store
        username: simkos@be-simkos.jaserba.store
        password: pusing081
        
    - name: PHP
      run: ls -a

  server-2:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 RGTESTING.site
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.rgtesting.site
        username: be-simkos@simkosapi.rgtesting.site
        password: pusing081

    # - name: PHP
    #   run: php artisan optimize:clear && php artisan storage:link


